![Makena, tenant un bloc de laine rose, s'approche d'un cube de soufre dans un biome de caverne de soufre. Derrière eux, on aperçoit un banc de poulpes luisants dans l'eau. On peut également voir de la lave, tant au premier plan que sous forme d'un écoulement tombant dans l'eau.](https://github.com/EclairDeFeu360/Minecraft-Changelog-in-French/blob/26.2-snap1/image.png)
## [Minecraft 26.2 Snapshot 1](https://www.minecraft.net/en-us/article/minecraft-26-2-snapshot-1)
- Le jeu est rendu via Vulkan, OpenGL peut toujours être utilisé
- Ajout d'une option vidéo « API graphique »
- Ajout du biome de cavernes de soufre
- Ajout de sources sulfureuses, des éléments de terrain qui se génèrent à la surface au-dessus des cavernes de soufre
- Ajout du cube de soufre, une créature qui change de physique en fonction du bloc qui lui est donné
- Ajout des blocs de cinabre, de soufre et de toutes leurs variantes
- Ajout du bloc de soufre concentré, qui, lorsqu'il est sous l'eau, produit un nuage provoquant des nausées
- La fréquence de vibration des entités qui rebondissent est de 2
- Le profilage du jeu avec Tracy inclut les temps d'exécution du GPU
- Le rendu utilise désormais un tampon de profondeur inversé
- Ajout du registre `minecraft:sulfur_cube_archetype`, qui définit un bloc et la physique qu'il appliquera sur un cube de soufre
- DP version 101.2
  - Lorsque plusieurs progrès sont attribués ou supprimés, la commande indique le nombre de progrès changés pour l'ensemble des joueurs
  - Lorsqu'un progrès ou un critère est attribué ou supprimé, la commande indique le nombre de joueurs concernés par ces modifications
  - Ajout des attributs :
    - `minecraft:bounciness` pour définir le taux de conservation de vélocité après un rebond
    - `minecraft:friction_modifier` pour définir le taux de friction du sol
    - `minecraft:air_drag_modifier` pour définir la friction dans l'air
  - La valeur minimale de l'attribut `minecraft:knockback_resistance` est de -2.0
  - Ajout du composant de donnée `minecraft:sulfur_cube_content` qui définit l'objet absorbé par les cubes de soufre
  - Les champs et les noms des éléments de terrain des stalactites ont été renommés pour remplacer « stalactites » par « spéléothèmes »
  - Ajout des champs `replaceable_blocks` dans les éléments de terrain `speleothem`, `speleothem_cluster` et `large_dripstone` pour définir sur quels blocs ces éléments de terrain peuvent se générer
  - Ajout des champs `base_block` et `pointed_block` dans les éléments de terrain `speleothem` et `speleothem_cluster` pour définir l'état du bloc de la spéléothème et les blocs constituant la colonne de la spéléothème
  - Ajout de la règle de surface `noise_gradient`, qui remplace les blocs en fonction d'un bruit et d'une liste de dégradés
  - Ajout du type d'élément de terrain `sequence`, qui génère des éléments de terrain placés dans un ordre spécifique
  - Ajout du type d'élément de terrain `template`, qui place un modèle choisi au hasard dans une liste pondérée
  - Le champ `infiniburn` dans les types de dimension accepte un ID ou une liste d'ID
  - Les champs des éléments de terrain configurés acceptent un ID ou une liste d'ID :
    - Les champs `cannot_replace` et `invalide_blocks` dans la section `blocks` des `minecraft:geode`
    - Le champ `root_replaceable` des `minecraft:root_system`
    - Les champs `replaceable` des `minecraft:vegetation_patch` et `minecraft:waterlogged_vegetation_patch`
  - Le processeur de structure `minecraft:protected_blocks` accepte un ID ou une liste d'ID
  - Ajout des tags de blocs et d'objets :
    - `#suppresses_bounce` qui définit les blocs qui suppriment le rebond des entités
    - `#glazed_terracotta` qui définit les blocs ou objets de terre cuite
    - `#concrete` qui définit les blocs ou objets de béton
    - `#shears_extreme_breaking_speed` qui définit les blocs pouvant être détruits avec des cisailles à une vitesse de 15
    - `#shears_major_breaking_speed` qui définit les blocs pouvant être détruits avec des cisailles à une vitesse de 5
    - `#shears_minor_breaking_speed` qui définit les blocs pouvant être détruits avec des cisailles à une vitesse de 2
    - `#concrete_powders` qui définit la liste des objets de poudre de béton
    - `#sulfur_cube_food` qui définit les objets qui peuvent nourrir un petit cube de soufre
    - `#sulfur_cube_swallowable` qui définit les objets qui peuvent être placés dans un cube de soufre
    - `#sulfur_cube_archetype/regular`, `#sulfur_cube_archetype/bouncy`, `#sulfur_cube_archetype/slow_flat`, `#sulfur_cube_archetype/fast_flat`, `#sulfur_cube_archetype/light`, `#sulfur_cube_archetype/fast_sliding`, `#sulfur_cube_archetype/slow_sliding`, `#sulfur_cube_archetype/high_resistance` et `#sulfur_cube_archetype/sticky` qui définissent les objets qui définissent les différents archétypes des cubes de soufre
  - Le tag de blocs `#concrete_powder` a été renommé en `#concrete_powders`
  - Ajout du tag `minecraft:sulfur_cube` au tag d'entité `#non_controlling_rider`
  - Ajout du tag d'entité `#minecraft:sulfur_cube_with_block_immune_to` qui définit tous les dégâts ignorés par les cubes de soufre lorsqu'ils ont un bloc
  - Ajout des cubes de soufre au tag d'entité `#not_scary_for_pufferfish`
- RP version 85.0
  - Ajout des sprites pour tous les nouveaux blocs et objets
  - Ajout des textures pour les cubes de soufre
  - Ajout de sons pour tous les blocs et cubes de soufre
  - Ajout de particules du cube de soufre lorsqu'il se déplace
  - Les shaders `core/rendertype_text`, `core/rendertype_text_see_through`, `core/rendertype_text_intensity`, `core/rendertype_text_intensity_see_through`, `core/rendertype_text_background` et `core/rendertype_text_background_see_through` ont été remplacés par `core/text` et `core/text_background`
  - Les variantes sont contrôlées par les définitions de shader `IS_GUI`, `IS_SEE_THROUGH` et `IS_GRAYSCALE`
- [30 bugs fixés](https://mojira.dev/?project=MC&fix_version=26.2-snapshot-1)